[project]
name = "renderman"
version = "0.1.0"
description = "Project for the Renderman Demos"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "ncca-ngl",
    "ruff",
]


